name: Experiment

on:
  push:


jobs:
  my-job:
    runs-on: ubuntu-latest

    env:
      TAG_NAME: ${GITHUB_REF##*/}

    steps:

      - name: Step 2
        run: |
          # Use the TAG_NAME environment variable
          echo "The tag name is: ${TAG_NAME}"
